<%= link_to 'Add Category', new_category_path %>

<div>
<% @categories.each do |category| %>
  <%= category.name %>
  <%= link_to 'Delete', category, method: :delete, id: "category-#{category.id}-destroy" %>
  <br>
<% end %>
</div>
